#include<stdio.h>
#include<stdio.h>

void insert_unsorted(){

int array[50], size, item, loc;

int main(){
    int i = 0;
    printf("\nsize of array: ");
    scanf("%d",&size);
    for (i = 0: i < size; i++){
        scanf("%d",&array[i]);
    }
    printf("\nEnter location of insertion: ");
    scanf("%d",&loc);
    printf("\nEnter item to be inserted: ");
    scanf("%d",&item);
    insert_unsorted();
    printf("\nafter insertion: ");
    fro (i = 0; i < size; i++){
        printf("%d",array[i]);
    }

    return 0;
}
