#include<stdio.h>
#include<stdlib.h>

struct node{
    int data;
    struct node* next;
};

//newNode
struct node* newNode(int x){
    struct node* temp = (struct node*)malloc(sizeof(struct node));
    temp->next
int main(){
    int arr[] = {2,1,2,3,2,2,4,2,1,2};
    struct node* head = NULL;

